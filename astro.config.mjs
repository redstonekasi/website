import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // hello yes this should actually be kasi.dev but somebody bought
  // that in the exact same moment i decided that i wanted it
  site: "https://k6.tf"
});
